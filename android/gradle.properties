# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Android build settings
android.useAndroidX=true
android.enableJetifier=true

# Kotlin code style
kotlin.code.style=official

# Java toolchain
# Gradle will automatically detect Java from JAVA_HOME environment variable
# To override locally, add org.gradle.java.home to ~/.gradle/gradle.properties
# DO NOT commit org.gradle.java.home - it's machine-specific
